"use strict";

define(['application-configuration'], function (app) {

    app.register.controller('headerController', ['$scope', function ($scope) {
        console.log('Header Controller running...');
    }]);

});

